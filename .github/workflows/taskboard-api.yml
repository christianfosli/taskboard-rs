name: taskboard-api

on:
  push:
    branches: [master]
    paths:
      - api/*
      - .github/workflows/taskboard-api.yml
  pull_request:
    branches: [master]
    paths:
      - api/*
      - .github/workflows/taskboard-api.yml

defaults:
  run:
    working-directory: api

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: login
        run: echo "$PASSWORD" | docker login -u "$USERNAME" --password-stdin
        env:
          USERNAME: ${{ secrets.DOCKER_USERNAME }}
          PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      - name: build
        run: |
          docker build \
            -t christianfosli/taskboard-api:"$GITHUB_SHA" \
            -t christianfosli/taskboard-api:latest .
        env:
          DOCKER_BUILDKIT: 1
      - name: push
        run: |
          docker push christianfosli/taskboard-api:"$GITHUB_SHA"
          docker push christianfosli/taskboard-api:latest
